<!DOCTYPE html>
<html>
<head>
  <title>Vanstaurant</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body data-no-turbolink>

  <% if notice %> 
    <p class="notice"><%= notice %></p>
  <% end %>

  <% if alert %>
    <p class="alert"><%= alert %></p>
  <% end %>

  <div class="navbar" style="height:80px; position:fixed; top: 0px; right: 0px; left: 0px; z-index: 2;">
    <% if current_user %>
      <div class="medium-top-margin">
        <div class="yellow-font helvetica inline medium-font inline"> 
          <%= "Logged in as " + current_user.name.upcase %>
        </div>
        <%= link_to "Log Out", destroy_user_session_path, method: :delete, class: "inline medium-left-margin btn btn-lg btn-default-clear" %>
      </div>
    <% end %>
  </div>

    <%= yield %>
</body>
</html>
