<!DOCTYPE html>
<html>
<head>
  <title>Vanstaurant</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>

  <% if notice %> 
    <p class="notice"><%= notice %></p>
  <% end %>

  <% if alert %>
    <p class="alert"><%= alert %></p>
  <% end %>

    <% if current_user %>
      <div class="inline admin-nav col-md-11 col-md-offset-1 medium-font medium-top-margin">
        <div class="yellow-font helvetica inline"> 
          <%= "Logged in as " + current_user.name.upcase %>
        </div>
        <%= link_to "Log Out", destroy_user_session_path, method: :delete, class: "medium-left-margin btn btn-lg btn-default-clear medium-font" %>
      </div>
    <% end %>

    <%= yield %>
</body>
</html>
